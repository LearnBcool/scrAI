# Índice de Conversas — scrapAI

> Carregado automaticamente no início de cada sessão (via `instructions` no
> `opencode.json`). Leia o arquivo completo de uma conversa em
> `conversations/` quando o assunto atual for relacionado.

| Data | Tema | Resumo | Arquivo |
|------|------|--------|---------|
| 2026-08-15 | Erro 502 + pesquisa de modelos/API Google/legalidade + criação desta memória | 502 = backend (uvicorn) rodando na 8001 enquanto o proxy do Vite aponta para 8000. Pesquisa: DeepSeek V4 Flash e GPT-5 mini como melhores custo/benefício; Places API (New) como fonte de descoberta de leads; modelo B2B viável sob LGPD com ressalvas. Modelo atual do backend: `gpt-4o-mini` (config.py). Relatórios completos salvos em `docs/research/`. | `conversations/2026-08-15-erro-502-pesquisa-modelo-e-memoria.md` |
